# Copy to `.env` and fill in. `.env` is git-ignored — never commit real keys.
# Loaded automatically by finsentiment.config (via python-dotenv) if present.

OPENAI_API_KEY=sk-...
DEEPSEEK_API_KEY=sk-...

# Optional: only needed for the exploratory news pull in
# notebooks/00_scratch_data_collection.ipynb
MARKETAUX_API_KEY=...
